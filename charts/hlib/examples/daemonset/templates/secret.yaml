{{- include "hlib.secret" (dict "context" . "override" "daemon.secret") -}}
{{- define "daemon.secret" -}}
stringData:
  POSTGRES_USERNAME: {{ .Values.daemon.postgres.username }}
  POSTGRES_PASSWORD: {{ .Values.daemon.postgres.password }}
{{- end -}}
