# @ignored
deploymentFirst:
  container:
    resourceTier: "m100Mi-c100m"
    port: 8080
    image:
      repository: nginx

# @ignored
deploymentSecond:
  container:
    resourceTier: "m250Mi-c250m"
    port: 8080
    image:
      repository: bitnami/nginx

multiDepl:
  postgres:
    # -- PostgreSQL URL.
    # @section -- multiDepl Application
    host: some.postgres.test.host
    # -- PostgreSQL user password.
    # @section -- multiDepl Application
    password: "SuperSecretP@ssvv0rd"
    # -- PostgreSQL user name.
    # @section -- multiDepl Application
    username: simple
